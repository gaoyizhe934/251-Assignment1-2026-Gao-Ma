# Java Text Editor

This directory is the Maven project root. Source code belongs in `src`, runtime configuration in `config`, CI workflows in `.github/workflows`, Docker configuration in `docker`, and quality-tool output in `reports`.
